import { Agdasima, Inter } from 'next/font/google';

export const inter = Inter({
  subsets: ['latin'],
  weight: ['400', '600', '800'],
});

export const agdasima = Agdasima({
  subsets: ['latin'],
  weight: ['400', '700'],
});
